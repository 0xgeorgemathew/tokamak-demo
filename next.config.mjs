// FILE: next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special configuration is needed.
  // The `transpilePackages` option is not required for these libraries in Next.js 14.
};

export default nextConfig;
